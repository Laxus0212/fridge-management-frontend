components:
  schemas:
    User:
      type: object
      properties:
        user_id:
          type: integer
        email:
          type: string
        username:
          type: string
        password:
          type: string
        family_id:
          type: integer
      required:
        - email
        - username
        - password
