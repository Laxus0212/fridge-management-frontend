components:
  schemas:
    Message:
      type: object
      properties:
        message_id:
          type: integer
        sender_id:
          type: integer
        family_id:
          type: integer
        message:
          type: string
        sent_at:
          type: string
          format: date-time
      required: [sender_id, family_id, message]
