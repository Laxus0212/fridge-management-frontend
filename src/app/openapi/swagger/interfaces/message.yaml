components:
  schemas:
    Message:
      type: object
      properties:
        message_id:
          type: string
        chat_id:
          type: integer
        sender_id:
          type: integer
        message:
          type: string
        sent_at:
          type: string
          format: date-time
      required: [chat_id, message]
